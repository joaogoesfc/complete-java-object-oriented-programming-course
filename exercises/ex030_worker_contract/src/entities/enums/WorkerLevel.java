package entities.enums;

public enum WorkerLevel {
	
	JUNIOR,
	MID_LEVEL,
	SENIOR;
	
	/*
	public static WorkerLevel parse(String level) {
		if (level.equals("JUNIOR")) {
			return JUNIOR;
		} else if (level.equals("MID_LEVEL")) {
		return MID_LEVEL;
		} else if (level.equals("SENIOR")) {
		return SENIOR;
		} else return null;
	}
	*/
}
